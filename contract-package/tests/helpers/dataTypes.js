// Mapping of data types defined in solidity contracts like Structs.

/**
 * Enum from `Validator.sol` for the different user statuses.
 */
const UserStatus = {
    "INVALID": 0,
    "VALID": 1
}

module.exports = {
    UserStatus
}
